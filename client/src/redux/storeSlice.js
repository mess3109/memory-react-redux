import { createSlice } from '@reduxjs/toolkit'

export const storeSlice = createSlice({
    name: game,
    initialState: {

    },
    reducers: ,
})

export const { increaseCount } = storeSlice.actions
export default storeSlice.reducer;